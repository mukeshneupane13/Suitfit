# SuitFlow Android

This is the offline Android wrapper for SuitFlow.

The app:

- bundles the try-on interface and suit assets inside the APK;
- starts with a password gate;
- supports Android camera/gallery file selection;
- does not require the hosted website to run.

GitHub Actions produces a signed debug APK for direct installation and an
unsigned optimized release APK for later store-grade signing.
